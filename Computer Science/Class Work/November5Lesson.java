import java.util.*;

public class November5Lesson {
   public static void main(String[] args) {
   Scanner in = new Scanner(System.in);
   if () {        // use == for integers and values
                  // use (s.equals('String')) for Strings
                  //use (s.equalsIgnoreCase('String')) if you don't care if it's upper or lower casing
                  //regardless, Java does not like spaces in strings. it will return false if new spaces are added
   
   }
   
   
   if() {
   
   } else {
   
   }
   
   int n = in.nextInt();
   if (1 <= n <= 10) {   //Java does not like more than one restriction or domain Ex: if(1 <= n <= 10) {
      if (n <= 10) {           // use two if statements
      System.out.println();      
   }
   }
   
   //cond1 && cond2   =  AND  (Will result in true if BOTH of the operands are true)
   //cond1 || cond2   = OR    (will result in true if AT LEAST ONE of the operands are true)
   //    !cond1       = NOT   (will result in true if the operand is FALSE)
   
   int k = in.nextInt();
   if (1 <= k && k <= 10) {
   
   }
   }
   }